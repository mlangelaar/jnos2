
  1) configure and compile jnos binaries

   make clean
   ./configure
   make

  * does not overwrite existing config.h file

  2) copy over command usage files

   cp jnos /jnos
   cp jnospwmgr /jnos
   cp -rp usage /jnos

  * assuming your JNOS runtime area is /jnos

